apiVersion: v1
kind: ConfigMap

metadata:
  name: backend-config

data:
  PORT: "5000"
  NODE_ENV: "development"
  JWT_EXPIRE: "1d"
  JWT_COOKIE_EXPIRE: "1"
  EASEBUZZ_ENV: "test"
  SMTP_HOST: "smtp.gmail.com"
  SMTP_PORT: "587"
  FROM_NAME: "Saaj"
  FROM_EMAIL: "tanveerchauhan471@gmail.com"
  FRONTEND_URL: "http://localhost:3000"
  REDIS_URL: "redis://localhost:6379"
